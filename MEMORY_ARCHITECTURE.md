Memory Strata and StorageStratumDescriptionStorage BackendTTL / DecayEpisodicRaw interactions and transcripts.Postgres (Append-only).Permanent.SemanticFacts, entities, and relationships.Graph DB (e.g., Neo4j/Apache Age).Decays if unconfirmed.ProceduralRules governing AI interaction style.Redis (Key-Value).Permanent until explicitly overwritten.EmotionalLongitudinal affective mapping.Postgres (TimeSeries).Permanent.World StateThe active cognitive context window.Redis (In-Memory Cache).Expires after 24 hours.2. Consolidation and PruningThe Memory Engine utilizes a nightly cron job known as the Consolidation Worker.Semantic memory is strengthened and locked at a 1.0 base confidence score if it is referenced more than 5 times.Inference confidence decays by 50% if unreferenced for 90 days. Nodes are archived and removed from the active World State if they fall below a 0.2 threshold.
